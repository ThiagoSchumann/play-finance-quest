import ImageLayer from './ImageLayer.js';
import TileLayer from './TileLayer.js';
import TileSet from './TileSet.js';
import { TiledMap, Layer } from '@rpgjs/tiled';
import { RpgRenderer } from '../Renderer.js';
import { Container, Graphics } from 'pixi.js';
export interface MapInfo extends TiledMap {
    layers: Layer[];
}
export default class TileMap extends Container {
    private data;
    private renderer;
    background: Graphics;
    tilewidth: number;
    tileheight: number;
    private frameRateAnimation;
    tilesets: TileSet[];
    layers: {
        [layerName: string]: TileLayer | ImageLayer;
    };
    private tilesLayer;
    private frameTile;
    constructor(data: MapInfo, renderer: RpgRenderer);
    getData(): MapInfo;
    setBackgroundColor(color: string): void;
    private create;
}
